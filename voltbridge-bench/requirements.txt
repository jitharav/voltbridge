python-can>=4.3
cantools>=40.0
pymodbus==3.6.9
# optional, only needed for --ws telemetry streaming AND the OCPP gateway/CSMS:
# websockets>=12.0
# optional, only needed for --mqtt telemetry streaming, Redfish gateway, OCPP gateway:
# paho-mqtt>=1.6
# optional, only needed for the ML anomaly detector (ml_anomaly_detector.py):
# scikit-learn>=1.3
# numpy>=1.24
# joblib>=1.3
